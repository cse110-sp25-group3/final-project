export function createCard(skills, skillAssessment, companyName, industry, jobTitle, location, pay, companyInfo, requirements, jobDescription) {
    const card = document.createElement('card');
    header.className = 'card';
    header.innerHTML = `
      


    `;

    return card;
  }
  