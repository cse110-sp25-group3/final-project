//should allow user to preview a template of the email they will send

//should have an input to let user write a custom email --> include placeholders for fields like company name, recruiter name etc 
//again preview which resume and cover letter is being used 

export function renderAutoApply(container) {
    container.innerHTML = `<h2>auto apply settings</h2>`;

  }