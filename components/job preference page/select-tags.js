//should be able to use for 
    //Preferred Roles
    //Preferred Fields
    //Preferred Location

export function generateTags(/*should take array of all tag names*/ tags){
    
    //should have a "+add" button/input --> based on input, show 6 tag options that match the input string 
    //otherwise, suggest 6 random tags

    //each tag should be formatted like a button --> when clicked, add the tag to a selectedTags array and display as selected
    
    //return the entire element and the selectedTags array
}